class User {
    constructor(name) {
        this.username = name;
        this.prevScore = 0;
    }
}

const user1 = new User(Tweag);

const users = [user1]



const userCheck = new Promise((resolve, reject) => {(name) {
        if(users.find(name => ))
        };
    }
})